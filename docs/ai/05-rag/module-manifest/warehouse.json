{
  "module": "Warehouse",
  "resource": "warehouses",
  "notes": {
    "enabled": true,
    "domain_relation": "Warehouse::notes()",
    "inverse_relation": "Note::warehouses() registered in WarehouseServiceProvider",
    "timeline": true,
    "pivot_table": "noteables",
    "pivot_columns": ["note_id", "noteable_type", "noteable_id", "tenant_id"],
    "pivot_timestamps": false
  },
  "builder_rules": [
    "For note-enabled resources, add both model-side and inverse note relations.",
    "Do not call withTimestamps() on morph pivots unless the pivot has created_at and updated_at.",
    "via_resource validation must explicitly accept custom resources such as warehouses."
  ]
}
